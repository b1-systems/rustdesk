name: Flutter Debian Build without packaging

on:
  release:
    types: [published]
  # for testing purposes, enable manual trigger
  workflow_dispatch:

jobs:
  build-debian-raw-package:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: env.VERSION
